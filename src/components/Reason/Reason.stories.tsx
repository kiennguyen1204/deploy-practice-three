import { StoryObj, Meta } from '@storybook/react';

// components
import WhatMake from 'components/Reason';

export default {
  title: 'Components/WhatMake',
  component: WhatMake
} as Meta;

type Story = StoryObj<typeof WhatMake>;

export const Default: Story = {};
