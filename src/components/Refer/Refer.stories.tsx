import { StoryObj, Meta } from '@storybook/react';

// components
import ReferFriend from 'components/Refer';

export default {
  title: 'Components/ReferFriend',
  component: ReferFriend
} as Meta;

type Story = StoryObj<typeof ReferFriend>;

export const Default: Story = {};
